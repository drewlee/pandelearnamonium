** start of script.js **

var displayTree = (tree) => console.log(JSON.stringify(tree, null, 2));
function Node(value) {
  this.value = value;
  this.left = null;
  this.right = null;
}
function BinarySearchTree() {
  this.root = null;
}
function isBinarySearchTree(tree) {
  // Only change code below this line
  if (!tree) {
    return false;
  }
  
  const root = tree.root || tree;
  
  if (root.value === undefined || root.left === undefined || root.right === undefined) {
    return false;
  }

  if (root.left !== null && (root.left.value >= root.value || !isBinarySearchTree(root.left))) {
    return false;
  }

  if (root.right !== null && (root.right.value < root.value || !isBinarySearchTree(root.right))) {
    return false;
  }
  
  return true;
  // Only change code above this line
}


** end of script.js **

